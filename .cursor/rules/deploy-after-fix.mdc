---
description: Deploy ל-Firebase רק לפי בקשה מפורשת — לא בסוף שיחה
alwaysApply: true
---

# מדיניות פריסה (Firebase)

**אל תריץ** `npm run deploy` או פריסה ל-Firebase **בסוף שיחה** או אחרי שינויי קוד — **אלא אם המשתמש ביקש במפורש** לפרוס / לעלות ל-Firebase / hosting.

**ברירת המחדל:** בסוף שיחה מתבצע **רק עדכון ל-GitHub** (ראה `git-push-after-changes.mdc`).

**כשהמשתמש מבקש לפרוס:** הרץ מתוך שורש הפרויקט:

```bash
npm run deploy
```
